<div class="container-fluid">
	<div class="row">
		<div class="product-grid col-sm-10 col-sm-offset-1">
			<h1>Featured Product</h1>
				<div class="product-desc">
					<%= @featured_product.name %><br>
					<%= @featured_product.description %><br>
					<%= @featured_product.colour %>
				</div>
		</div>
</div>